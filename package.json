{
  "name": "portfolio-site-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node ./scripts/generate.js",
    "generate-blog": "node ./scripts/generate.js --blog-only",
    "generate-portfolio": "node ./scripts/generate.js --portfolio-only"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "fs-extra": "^11.1.1",
    "puppeteer": "^21.0.0"
  }
}
